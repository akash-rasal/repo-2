name: Test script 
on:
    push:
        branches:
            - main 
jobs:
    example-word-job:
        runs-on: ubuntu-latest
        steps:
            - name: Checkout code
              uses: actions/checkout@v2
            - name: print message
              run: echo "Hello world"

